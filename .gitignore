# Wails build output
build/bin/
frontend/dist/
frontend/wailsjs/

# Frontend deps
node_modules/
frontend/node_modules/
frontend/package.json.md5

# Go build artifacts at repo root
/upmark
/upmark.exe
/upmark-*.exe

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Local env
.env
.env.local

# Local-only working notes, kickoff/handoff docs, scratch
local/

# Claude Code working dir (per-session state, scheduled tasks, etc.)
.claude/

# MCPB build artifact — produced by `cd bridge && npm run pack`, attached to
# GitHub releases via the release workflow rather than tracked here.
*.mcpb
